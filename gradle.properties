# Reuse the result of configuring the build between runs, so repeated builds skip
# straight to running tasks. Gradle suggests this, and every task in this project
# stores and reuses a cache entry without complaint.
org.gradle.configuration-cache=true
